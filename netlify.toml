[build]
  command = "npm run build"
  publish = "."

[context]
  # Netlify will deploy the repository root as a static site.
  # `npm run build` compiles TypeScript sources (spofix/script.ts -> spofix/script.js)
