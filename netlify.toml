[build]
  command = "npm run build"
  publish = "public"

[context]
  # Netlify will deploy the public directory as a static site.
  # `npm run build` compiles TypeScript sources (src/spofix/script.ts -> public/assets/js/spofix/script.js)
